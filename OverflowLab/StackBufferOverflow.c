#include <stdio.h>

int main(){
	char input[32];
	char scret[32] = {"0"};

	printf("input your string: ");
	scanf("%s", input);
	printf("input data = %s\n", input);
	printf("scret data = %s\n", scret);

	return 0;
}
